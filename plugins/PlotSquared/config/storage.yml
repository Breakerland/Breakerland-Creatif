prefix: ''

# SQLite section
sqlite:
  # Should SQLite be used?
  use: false
  # The file to use
  db: "storage"

# MySQL section
mysql:
  # Should MySQL be used?
  use: true
  host: "$mysql_host"
  port: "$mysql_port"
  user: "$mysql_user_$server_name"
  password: "$mysql_password_$server_name"
  database: "$mysql_database_$server_name"
  # Set additional properties: https://goo.gl/wngtN8
  properties: 
  - "useSSL=true"
